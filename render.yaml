services:
  - type: web
    name: watchwise
    runtime: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      uvicorn app.server:app --host 0.0.0.0 --port $PORT